---
title: "Elections - API Docs | ElectionData.MY"
pageTitle: "Elections"
breadcrumb: "Endpoints"
description: "Retrieve party aggregates, seat-level winner data, and turnout statistics for any general or state election in Malaysia."
keywords: "API, elections, general election, state election, party results, seat results, turnout, Malaysia"
---

## Use Case

Use these endpoints to build an elections explorer, similar to the one on our [Elections dashboard](https://www.electiondata.my/elections). A user picks an election from a dropdown, and your app fetches the party breakdown, the seat-by-seat winner map, and the headline turnout statistics. The four endpoints work together: start with `/elections/dropdown` to get valid election identifiers, then pass them to whichever of the three data endpoints you need.

Just follow the 4 simple steps below, and you'll be up and running!

## Prerequisite: API Key

All requests to this API require a Bearer token. Requests without a token (or with an invalid one) will return a `401 Unauthorized` error. Paste yours below—every code snippet and the live testers at the bottom of this page will be pre-filled and ready to run.

<TokenInput />

## Step 1: Get list of elections

<EndpointBadge method="GET" url="https://api.electiondata.my/v1/elections/dropdown" />

This endpoint takes no parameters and returns every general and state election in the database. The `state` and `election` values it returns are the exact parameters required by Steps 2–4. Because the list is only updated when a new election is called, we recommend fetching it once and caching it for the duration of a session (or bundling it at build time for static pages).

<TabCode
  curl={`curl -X GET "https://api.electiondata.my/v1/elections/dropdown" \\\n  -H "Authorization: Bearer <your-api-key>"`}
  javascript={`const response = await fetch(\n  "https://api.electiondata.my/v1/elections/dropdown",\n  {\n    headers: {\n      "Authorization": "Bearer <your-api-key>"\n    }\n  }\n);\n\nconst data = await response.json();\nconsole.log(data);`}
  typescript={`const response = await fetch(\n  "https://api.electiondata.my/v1/elections/dropdown",\n  {\n    headers: {\n      Authorization: "Bearer <your-api-key>",\n    },\n  }\n);\n\ninterface Election {\n  state: string;\n  type: "parlimen" | "dun";\n  election: string;\n  date: string;\n}\n\ninterface ElectionsDropdownResponse {\n  elections: Election[];\n}\n\nconst data: ElectionsDropdownResponse = await response.json();\nconsole.log(data);`}
  python={`import requests\n\nresponse = requests.get(\n    "https://api.electiondata.my/v1/elections/dropdown",\n    headers={"Authorization": "Bearer <your-api-key>"}\n)\n\ndata = response.json()\nprint(data)`}
  r={`library(httr2)\n\nresp <- request("https://api.electiondata.my/v1/elections/dropdown") |>\n  req_headers(Authorization = "Bearer <your-api-key>") |>\n  req_perform()\n\ndata <- resp_body_json(resp)\nprint(data)`}
  go={`package main\n\nimport (\n\t"encoding/json"\n\t"fmt"\n\t"io"\n\t"net/http"\n)\n\nfunc main() {\n\treq, _ := http.NewRequest("GET",\n\t\t"https://api.electiondata.my/v1/elections/dropdown", nil)\n\treq.Header.Set("Authorization", "Bearer <your-api-key>")\n\n\tresp, _ := http.DefaultClient.Do(req)\n\tdefer resp.Body.Close()\n\n\tbody, _ := io.ReadAll(resp.Body)\n\tvar data any\n\tjson.Unmarshal(body, &data)\n\tfmt.Println(data)\n}`}
/>

A successful `200 OK` response returns a JSON object with a single `elections` key containing an array of every election. Each object contains:

<FieldTable fields={[
  { name: "state", type: "string", description: "State or aggregation scope (e.g. 'Malaysia', 'Semenanjung', 'Selangor'). Pass this to Steps 2–4." },
  { name: "type", type: "string", description: "'parlimen' for parliamentary elections or 'dun' for state assembly elections." },
  { name: "election", type: "string", description: "Election identifier (e.g. 'GE-15' or 'SE-15'). Pass this to Steps 2–4." },
  { name: "date", type: "date", description: "Polling day in ISO 8601 format (YYYY-MM-DD)." },
]} />

## Step 2: Get results by party

Pass the `state` and `election` from Step 1 to get party-level vote and seat aggregates.

<EndpointCard method="GET" url="https://api.electiondata.my/v1/elections/by_party" params={[
  {
    name: "state",
    type: "string",
    required: true,
    description: "State or aggregation scope, e.g. 'Malaysia' or 'Selangor'. Taken directly from the state field in the dropdown response."
  },
  {
    name: "election",
    type: "string",
    required: true,
    description: "Election identifier, e.g. 'GE-15' or 'SE-15'. Taken directly from the election field in the dropdown response."
  }
]} />

<TabCode
  curl={`curl -X GET "https://api.electiondata.my/v1/elections/by_party?state=Malaysia&election=GE-15" \\\n  -H "Authorization: Bearer <your-api-key>"`}
  javascript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/by_party?\${params}\`,\n  {\n    headers: {\n      "Authorization": "Bearer <your-api-key>"\n    }\n  }\n);\n\nconst data = await response.json();\nconsole.log(data);`}
  typescript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/by_party?\${params}\`,\n  {\n    headers: {\n      Authorization: "Bearer <your-api-key>",\n    },\n  }\n);\n\ninterface PartyResult {\n  party_uid: string;\n  party: string;\n  coalition: string;\n  coalition_uid: string;\n  seats_contested: number;\n  seats_won: number;\n  seats_total: number;\n  seats_contested_perc: number;\n  seats_won_perc: number;\n  votes: number;\n  votes_total: number;\n  votes_perc: number;\n}\n\ninterface ByPartyResponse {\n  by_party: PartyResult[];\n}\n\nconst data: ByPartyResponse = await response.json();\nconsole.log(data);`}
  python={`import requests\n\nresponse = requests.get(\n    "https://api.electiondata.my/v1/elections/by_party",\n    params={\n        "state": "Malaysia",\n        "election": "GE-15",\n    },\n    headers={"Authorization": "Bearer <your-api-key>"}\n)\n\ndata = response.json()\nprint(data)`}
  r={`library(httr2)\n\nresp <- request("https://api.electiondata.my/v1/elections/by_party") |>\n  req_url_query(\n    state = "Malaysia",\n    election = "GE-15"\n  ) |>\n  req_headers(Authorization = "Bearer <your-api-key>") |>\n  req_perform()\n\ndata <- resp_body_json(resp)\nprint(data)`}
  go={`package main\n\nimport (\n\t"encoding/json"\n\t"fmt"\n\t"io"\n\t"net/http"\n\t"net/url"\n)\n\nfunc main() {\n\tparams := url.Values{}\n\tparams.Set("state", "Malaysia")\n\tparams.Set("election", "GE-15")\n\n\treq, _ := http.NewRequest("GET",\n\t\t"https://api.electiondata.my/v1/elections/by_party?"+params.Encode(), nil)\n\treq.Header.Set("Authorization", "Bearer <your-api-key>")\n\n\tresp, _ := http.DefaultClient.Do(req)\n\tdefer resp.Body.Close()\n\n\tbody, _ := io.ReadAll(resp.Body)\n\tvar data any\n\tjson.Unmarshal(body, &data)\n\tfmt.Println(data)\n}`}
/>

A successful `200 OK` response returns a JSON object with a `by_party` key containing an array of party results, ordered by votes received (descending). Each object contains:

<FieldTable fields={[
  { name: "party_uid", type: "string", description: "Unique party identifier (e.g. '004-PAS')." },
  { name: "party", type: "string", description: "Party acronym (e.g. 'PAS')." },
  { name: "coalition", type: "string", description: "Coalition name (e.g. 'PN'). 'ALONE' if the party contested independently." },
  { name: "coalition_uid", type: "string", description: "Unique coalition identifier (e.g. '11-PN'). '0' if independent." },
  { name: "seats_contested", type: "integer", description: "Number of seats the party contested in this election." },
  { name: "seats_won", type: "integer", description: "Number of seats the party won." },
  { name: "seats_total", type: "integer", description: "Total seats available in this election (the denominator for share calculations)." },
  { name: "seats_contested_perc", type: "float", description: "seats_contested as a percentage of seats_total." },
  { name: "seats_won_perc", type: "float", description: "seats_won as a percentage of seats_total." },
  { name: "votes", type: "integer", description: "Total votes received by the party across all seats." },
  { name: "votes_total", type: "integer", description: "Total valid votes cast in the election (the denominator for vote share)." },
  { name: "votes_perc", type: "float", description: "votes as a percentage of votes_total." },
]} />

## Step 3: Get results by seat

Pass the same `state` and `election` to get data constituency in that election.

<EndpointCard method="GET" url="https://api.electiondata.my/v1/elections/by_seat" params={[
  {
    name: "state",
    type: "string",
    required: true,
    description: "State or aggregation scope, e.g. 'Malaysia' or 'Selangor'. Taken directly from the state field in the dropdown response."
  },
  {
    name: "election",
    type: "string",
    required: true,
    description: "Election identifier, e.g. 'GE-15' or 'SE-15'. Taken directly from the election field in the dropdown response."
  }
]} />

<TabCode
  curl={`curl -X GET "https://api.electiondata.my/v1/elections/by_seat?state=Malaysia&election=GE-15" \\\n  -H "Authorization: Bearer <your-api-key>"`}
  javascript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/by_seat?\${params}\`,\n  {\n    headers: {\n      "Authorization": "Bearer <your-api-key>"\n    }\n  }\n);\n\nconst data = await response.json();\nconsole.log(data);`}
  typescript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/by_seat?\${params}\`,\n  {\n    headers: {\n      Authorization: "Bearer <your-api-key>",\n    },\n  }\n);\n\ninterface SeatResult {\n  seat: string;\n  state: string;\n  date: string;\n  name: string;\n  party: string;\n  party_uid: string;\n  coalition: string;\n  coalition_uid: number;\n  party_lost: string[];\n  party_lost_uid: string[];\n  coalition_lost: string[];\n  coalition_lost_uid: number[];\n  n_candidates: number;\n  voters_total: number;\n  voter_turnout: number;\n  voter_turnout_perc: number;\n  majority: number;\n  majority_perc: number;\n  votes_rejected: number;\n  votes_rejected_perc: number;\n}\n\ninterface BySeatResponse {\n  by_seat: SeatResult[];\n}\n\nconst data: BySeatResponse = await response.json();\nconsole.log(data);`}
  python={`import requests\n\nresponse = requests.get(\n    "https://api.electiondata.my/v1/elections/by_seat",\n    params={\n        "state": "Malaysia",\n        "election": "GE-15",\n    },\n    headers={"Authorization": "Bearer <your-api-key>"}\n)\n\ndata = response.json()\nprint(data)`}
  r={`library(httr2)\n\nresp <- request("https://api.electiondata.my/v1/elections/by_seat") |>\n  req_url_query(\n    state = "Malaysia",\n    election = "GE-15"\n  ) |>\n  req_headers(Authorization = "Bearer <your-api-key>") |>\n  req_perform()\n\ndata <- resp_body_json(resp)\nprint(data)`}
  go={`package main\n\nimport (\n\t"encoding/json"\n\t"fmt"\n\t"io"\n\t"net/http"\n\t"net/url"\n)\n\nfunc main() {\n\tparams := url.Values{}\n\tparams.Set("state", "Malaysia")\n\tparams.Set("election", "GE-15")\n\n\treq, _ := http.NewRequest("GET",\n\t\t"https://api.electiondata.my/v1/elections/by_seat?"+params.Encode(), nil)\n\treq.Header.Set("Authorization", "Bearer <your-api-key>")\n\n\tresp, _ := http.DefaultClient.Do(req)\n\tdefer resp.Body.Close()\n\n\tbody, _ := io.ReadAll(resp.Body)\n\tvar data any\n\tjson.Unmarshal(body, &data)\n\tfmt.Println(data)\n}`}
/>

A successful `200 OK` response returns a JSON object with a `by_seat` key containing an array of seat results, one entry per constituency. Each object contains:

<FieldTable fields={[
  { name: "seat", type: "string", description: "Constituency identifier (e.g. 'P.001 Padang Besar, Perlis')." },
  { name: "state", type: "string", description: "The state parameter echoed back." },
  { name: "date", type: "date", description: "Polling day for this seat in ISO 8601 format (YYYY-MM-DD)." },
  { name: "name", type: "string", description: "Winner's full name." },
  { name: "party", type: "string", description: "Acronym of the winning party (e.g. 'PAS')." },
  { name: "party_uid", type: "string", description: "Unique identifier for the winning party (e.g. '004-PAS')." },
  { name: "coalition", type: "string", description: "Coalition of the winning party (e.g. 'PN'). 'ALONE' if independent." },
  { name: "coalition_uid", type: "integer", description: "Numeric coalition identifier (0 if independent)." },
  { name: "party_lost", type: "string[]", description: "Acronyms of parties that lost in this seat." },
  { name: "party_lost_uid", type: "string[]", description: "Unique identifiers for the losing parties." },
  { name: "coalition_lost", type: "string[]", description: "Coalitions represented by the losing parties." },
  { name: "coalition_lost_uid", type: "integer[]", description: "Numeric identifiers for the losing coalitions." },
  { name: "n_candidates", type: "integer", description: "Total number of candidates who contested this seat." },
  { name: "voters_total", type: "integer", description: "Total number of registered voters for this seat." },
  { name: "voter_turnout", type: "integer", description: "Total votes cast, including rejected votes." },
  { name: "voter_turnout_perc", type: "float", description: "Turnout as a percentage of registered voters." },
  { name: "majority", type: "integer", description: "Winning margin, i.e. winner's votes minus runner-up's votes." },
  { name: "majority_perc", type: "float", description: "Majority as a percentage of total valid votes." },
  { name: "votes_rejected", type: "integer", description: "Number of votes rejected during counting." },
  { name: "votes_rejected_perc", type: "float", description: "Rejected votes as a percentage of total ballots cast." },
]} />

## Step 4: Get summary stats

Pass the same `state` and `election` to get aggregate turnout and rejection statistics for the entire election.

<EndpointCard method="GET" url="https://api.electiondata.my/v1/elections/stats" params={[
  {
    name: "state",
    type: "string",
    required: true,
    description: "State or aggregation scope, e.g. 'Malaysia' or 'Selangor'. Taken directly from the state field in the dropdown response."
  },
  {
    name: "election",
    type: "string",
    required: true,
    description: "Election identifier, e.g. 'GE-15' or 'SE-15'. Taken directly from the election field in the dropdown response."
  }
]} />

<TabCode
  curl={`curl -X GET "https://api.electiondata.my/v1/elections/stats?state=Malaysia&election=GE-15" \\\n  -H "Authorization: Bearer <your-api-key>"`}
  javascript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/stats?\${params}\`,\n  {\n    headers: {\n      "Authorization": "Bearer <your-api-key>"\n    }\n  }\n);\n\nconst data = await response.json();\nconsole.log(data);`}
  typescript={`const params = new URLSearchParams({\n  state: "Malaysia",\n  election: "GE-15",\n});\n\nconst response = await fetch(\n  \`https://api.electiondata.my/v1/elections/stats?\${params}\`,\n  {\n    headers: {\n      Authorization: "Bearer <your-api-key>",\n    },\n  }\n);\n\ninterface ElectionStats {\n  voters_total: number;\n  voter_turnout: number;\n  voter_turnout_perc: number;\n  votes_rejected: number;\n  votes_rejected_perc: number;\n  n_candidates: number;\n}\n\ninterface StatsResponse {\n  stats: ElectionStats[];\n}\n\nconst data: StatsResponse = await response.json();\nconsole.log(data);`}
  python={`import requests\n\nresponse = requests.get(\n    "https://api.electiondata.my/v1/elections/stats",\n    params={\n        "state": "Malaysia",\n        "election": "GE-15",\n    },\n    headers={"Authorization": "Bearer <your-api-key>"}\n)\n\ndata = response.json()\nprint(data)`}
  r={`library(httr2)\n\nresp <- request("https://api.electiondata.my/v1/elections/stats") |>\n  req_url_query(\n    state = "Malaysia",\n    election = "GE-15"\n  ) |>\n  req_headers(Authorization = "Bearer <your-api-key>") |>\n  req_perform()\n\ndata <- resp_body_json(resp)\nprint(data)`}
  go={`package main\n\nimport (\n\t"encoding/json"\n\t"fmt"\n\t"io"\n\t"net/http"\n\t"net/url"\n)\n\nfunc main() {\n\tparams := url.Values{}\n\tparams.Set("state", "Malaysia")\n\tparams.Set("election", "GE-15")\n\n\treq, _ := http.NewRequest("GET",\n\t\t"https://api.electiondata.my/v1/elections/stats?"+params.Encode(), nil)\n\treq.Header.Set("Authorization", "Bearer <your-api-key>")\n\n\tresp, _ := http.DefaultClient.Do(req)\n\tdefer resp.Body.Close()\n\n\tbody, _ := io.ReadAll(resp.Body)\n\tvar data any\n\tjson.Unmarshal(body, &data)\n\tfmt.Println(data)\n}`}
/>

A successful `200 OK` response returns a JSON object with a `stats` key containing an array with a single aggregate statistics object:

<FieldTable fields={[
  { name: "voters_total", type: "integer", description: "Total number of registered voters across all seats in this election." },
  { name: "voter_turnout", type: "integer", description: "Total votes cast, including rejected votes." },
  { name: "voter_turnout_perc", type: "float", description: "Turnout as a percentage of registered voters." },
  { name: "votes_rejected", type: "integer", description: "Total number of votes rejected across all seats." },
  { name: "votes_rejected_perc", type: "float", description: "Rejected votes as a percentage of total ballots cast." },
  { name: "n_candidates", type: "integer", description: "Total number of candidates who contested across all seats." },
]} />

## Try It Out

Start on the **Dropdown** tab to fetch the list of elections, then switch to **By Party**, **By Seat**, or **Stats**—load the elections once and the selection stays synced across all tabs.

<ElectionsMainApiTester />
